# Model dependencies resolved with Reactor Runtime during `reactor build`.
# `build.runtime_version` in reactor.yaml selects the Runtime release.
#
# Pinned to the same lineage as fast-h3: the merged ai-toolkit training
# commit c5559066481fc6aede69f2385a2351b8ad8882ba and PR #17 head
# a472c2e44937c88cbf34f89ef1d465e2cd0ab71c. The causal trunk, PDD heads,
# and control branch are native components of the trained merged family
# export, loaded through the pinned FastVideo components below.
#
# The ai-toolkit H3 runtime subset (transformer, causal, packing, VAEs,
# text encoder, pipeline, artifacts) is vendored under causal-h3/minimax_h3/
# from PR #17 head (a472c2e). The vendored transformer has the update_cache
# flag that the persistent serving model needs for read-only denoise forwards.
# No pip install is needed for the vendored code; it is on sys.path via
# the causal-h3 directory (PYTHONPATH or sitecustomize.py).
--extra-index-url https://download.pytorch.org/whl/cu130

# The cu130 builds the sparse-attention and FA4 kernels are compiled against.
# 2.12.0 is what fastvideo 0.2.1 pins. torchvision and torchaudio are pinned
# exactly because UV_OVERRIDE strips their own torch coupling from resolution.
torch==2.12.0
torchaudio==2.11.0
torchvision==0.27.1

# The inference stack: pipelines, the MiniMax-H3 model code, the VSA-H3
# attention backend, and the VideoGenerator engine.
fastvideo==0.2.1

# The Blackwell sparse-attention kernel, built from source at the same
# pinned release commit as fast-h3. See fast-h3/requirements.txt for the
# full rationale (driver 595 sm_100a wheel failure, sdist submodule gap).
fastvideo-kernel @ git+https://github.com/hao-ai-lab/FastVideo.git@1aed66737739e1fd6ea7271d3306a615a2a6bd55#subdirectory=fastvideo-kernel

# FA4 CuTe kernels for the dense (text and audio) attention paths.
# Pinned to the same revision as fast-h3.
flash-attn-4 @ git+https://github.com/Dao-AILab/flash-attention.git@14c377950125c70b7a9dabf9c561fca53715ac7d#subdirectory=flash_attn/cute

# Pinned because cutlass-dsl caps protobuf at <7 while the Reactor Runtime
# needs >=7.35.1. build_env sets UV_OVERRIDE to this file.
protobuf>=7.35.1

# causalh3_assets.py parses causalh3.yaml and model-package.json.
pyyaml

# jsonschema is required for model-package-v1 manifest validation.
jsonschema>=4.0.0
